# Google OAuth — get from console.cloud.google.com
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Random 32-char string — generate with: openssl rand -hex 16
AUTH_SECRET=

# Cloudflare (auto-injected in Pages, only needed for local dev reference)
APP_URL=http://localhost:4321